
ktor:
  application:
    modules:
      - org.burgas.ApplicationKt.module
  deployment:
    port: 9010
  postgres:
    driver: "org.postgresql.Driver"
    user: "postgres"
    password: "postgres"
    url: "jdbc:postgresql://localhost:5010/kotlin_ktor_db"
  redis:
    host: "localhost"
    port: 6010